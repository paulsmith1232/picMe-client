import React from 'react';

// The main feed component - will serve as the homepage
const Profile = () => {
  return (
    <div>
      <h1>Profile Page</h1>
    </div>
  );
};

export default Profile;